# core/__init__.py — واجهة موحدة للوحدات المساعدة في مشروع بسام الذكي

from .utils import (
    ensure_dirs,
    dedup_by_url,
    clamp,
    simple_md_search,
    extract_image_text,
    convert_arabic_numbers,
    is_arabic,
    clean_html,
    normalize_spaces,
    normalize_text,
)
